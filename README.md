Demonstration of React hooks using **Node.js** and **Next.js**.

Dependency management was initialized with:

```npm init -y```

**ReactJS**, **ReactDOM** and **Next.js** are installed locally with:

```npm install react react-dom next```